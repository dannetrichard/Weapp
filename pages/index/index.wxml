<view class="container">
    <scroll-view  scroll-y="true" style="height:{{windowHeight}}px;" bindscrolltoupper="upper" bindscrolltolower="lower" scroll-top="{{scrollTop}}">
    <view class="list">
            <view class="item" wx:for="{{products}}" wx:key="item">
                <image class="pic" mode="aspectFill"  src="{{item.pic_url+'_400x400'}}" />
            </view>
            </view>
    </scroll-view>
</view>
